<!-- Loop over each object in the tournament2s array -->
{{#each tournament2s as |tournament2| }}
<div class="row mb-4 tournament2">
  <div class="col-md-5">
    <h2>
      {{!-- <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}} --}}
      <a href="/tournament2/{{tournament2.id}}">{{tournament2.tournament2_name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="money">🎮</span>
      Minimum<p> </p>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount tournament2.player_quantity}}</span>
      players!
    </p>
    <p>Created by {{tournament2.user.name}} on {{format_date tournament2.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{tournament2.description}}
    </p>
  </div>
</div>
{{/each}}